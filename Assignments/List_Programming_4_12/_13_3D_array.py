# Write 3D array list python
import pprint
                #element of the array      # Colums of array
array1 = [[ ['$' for col in range(3)] for col in range(3)] for row in range(3)]
pprint.pprint(array1)